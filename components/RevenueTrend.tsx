export function RevenueTrend({payments}:{payments:Array<{payment_date:string;amount:number}>}){const months=Array.from({length:6},(_,i)=>{const d=new Date();d.setUTCDate(1);d.setUTCMonth(d.getUTCMonth()-(5-i));return{key:`${d.getUTCFullYear()}-${String(d.getUTCMonth()+1).padStart(2,"0")}`,label:d.toLocaleString("en-US",{month:"short"})}});const totals=months.map(m=>payments.filter(p=>p.payment_date.startsWith(m.key)).reduce((s,p)=>s+Number(p.amount||0),0));const max=Math.max(...totals,0);return <div className="vx-surface rounded-2xl p-5"><div className="vx-kicker">Revenue trend</div><h2 className="mt-1 text-base font-semibold text-white">Received payments · last 6 months</h2>{payments.length===0?<div className="mt-6 text-sm text-slate-500">No payment history has been recorded yet.</div>:<div className="mt-6 grid h-40 grid-cols-6 items-end gap-3">{totals.map((v,i)=><div key={months[i].key} className="flex h-full flex-col justify-end gap-2"><div className="text-center text-[10px] text-slate-500">{v?Intl.NumberFormat("en-IN",{notation:"compact"}).format(v):"—"}</div><div className="rounded-t-lg bg-white/80" style={{height:`${max?Math.max((v/max)*110,4):4}px`}}/><div className="text-center text-[10px] uppercase tracking-wide text-slate-600">{months[i].label}</div></div>)}</div>}</div>}