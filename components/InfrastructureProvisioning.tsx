"use client";
import { useState } from "react";
import { createInfrastructureServer, createN8nInstance } from "@/lib/infrastructure-actions";
export function InfrastructureProvisioning({servers}:{servers:any[]}){const[b,setBusy]=useState(false),[e,setError]=useState<string|null>(null);const submit=(fn:(f:FormData)=>Promise<unknown>)=>async(f:FormData)=>{setBusy(true);setError(null);try{await fn(f);location.reload()}catch(x){setError(x instanceof Error?x.message:"Could not save infrastructure record.");setBusy(false)}};return <div className="grid gap-4 lg:grid-cols-2"><form action={submit(createInfrastructureServer)} className="vx-surface rounded-2xl p-5"><div className="vx-kicker">Add server</div><div className="mt-4 grid gap-3 sm:grid-cols-2"><input required name="provider_name" placeholder="Provider" className="vx-field"/><input required name="server_name" placeholder="Server name" className="vx-field"/><input name="hostname" placeholder="Hostname" className="vx-field"/><input name="region" placeholder="Region" className="vx-field"/><input name="monthly_cost" type="number" min="0" step="0.01" placeholder="Monthly cost" className="vx-field"/><input name="renewal_date" type="date" className="vx-field"/><textarea name="notes" placeholder="Operational notes" className="vx-field min-h-24 sm:col-span-2"/><button disabled={b} className="rounded-xl bg-white px-4 py-2.5 text-sm font-bold text-slate-950 sm:col-span-2">{b?"Saving…":"Save server"}</button></div></form><form action={submit(createN8nInstance)} className="vx-surface rounded-2xl p-5"><div className="vx-kicker">Add n8n instance</div><div className="mt-4 grid gap-3 sm:grid-cols-2"><input required name="instance_name" placeholder="Instance name" className="vx-field"/><input required name="base_url" placeholder="https://n8n.example.com" className="vx-field"/><select name="hosting_type" defaultValue="shared" className="vx-field"><option value="shared">Shared</option><option value="dedicated">Dedicated</option></select><select name="server_id" defaultValue="" className="vx-field"><option value="">No server relation</option>{servers.map(s=><option key={s.id} value={s.id}>{s.server_name}</option>)}</select><div className="rounded-xl border border-amber-300/10 bg-amber-300/[.03] p-3 text-xs leading-5 text-amber-100/75 sm:col-span-2">Safe metadata only. Raw n8n API credentials are not accepted or stored here.</div><button disabled={b} className="rounded-xl bg-white px-4 py-2.5 text-sm font-bold text-slate-950 sm:col-span-2">{b?"Saving…":"Save instance"}</button></div></form>{e&&<div role="alert" className="text-sm text-red-300 lg:col-span-2">{e}</div>}</div>}
