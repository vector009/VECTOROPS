function tone(value:number){return value>=.9?"text-emerald-300":value>=.65?"text-amber-200":"text-red-300"}
export function HealthMatrix({operational,billing,relationship,results}:{operational:number;billing:number;relationship:number;results:number}){const rows:[[string,number],[string,number],[string,number],[string,number]]=[["Operational",operational],["Billing",billing],["Relationship",relationship],["Results / usage",results]];return <div className="vx-surface rounded-2xl p-5"><div className="vx-kicker">Client health</div><p className="mt-1 text-sm text-slate-500">Independent dimensions; no opaque composite score.</p><div className="mt-5 space-y-4">{rows.map(([label,v])=><div key={label}><div className="flex justify-between text-xs"><span className="text-slate-400">{label}</span><span className={tone(v)}>{Math.round(v*100)}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white/5"><div className="h-full rounded-full bg-white/80" style={{width:`${Math.round(v*100)}%`}}/></div></div>)}</div></div>}