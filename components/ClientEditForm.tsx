"use client";
import { useState } from "react";
import { updateClient } from "@/lib/actions";
export function ClientEditForm({client}:{client:any}){const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null);async function submit(fd:FormData){setBusy(true);setError(null);try{await updateClient(client.id,fd);window.location.reload()}catch(e){setError(e instanceof Error?e.message:"Client could not be updated.");setBusy(false)}}return <form action={submit} className="mt-4 grid gap-3 sm:grid-cols-2"><input name="company_name" defaultValue={client.company_name} required className="vx-field" placeholder="Company"/><input name="contact_name" defaultValue={client.contact_name||""} className="vx-field" placeholder="Contact"/><input name="email" type="email" defaultValue={client.email||""} className="vx-field" placeholder="Email"/><input name="phone" defaultValue={client.phone||""} className="vx-field" placeholder="Phone"/><select name="status" defaultValue={client.status} className="vx-field"><option value="pending">pending</option><option value="active">active</option><option value="paused">paused</option><option value="churned">churned</option><option value="archived">archived</option></select><textarea name="notes" defaultValue={client.notes||""} rows={3} className="vx-field sm:col-span-2" placeholder="Notes"/><button disabled={busy} className="rounded-xl bg-white px-4 py-2.5 text-xs font-bold text-slate-950 sm:col-span-2">{busy?"Saving…":"Save client"}</button>{error&&<div role="alert" className="text-xs text-red-300 sm:col-span-2">{error}</div>}</form>}
