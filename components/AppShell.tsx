import { Bell, Menu, Search } from "lucide-react";
import { LogoutButton } from "@/components/LogoutButton";
import { AppNav } from "@/components/AppNav";
import type { LucideIcon } from "lucide-react";

export function AppShell({ children, nav, current, title, subtitle, identity, mobileNav }: { children:React.ReactNode; nav:Array<{href:string,label:string,icon:LucideIcon}>; current:string; title:string; subtitle?:string; identity:string; mobileNav?:boolean }) {
  return <div className="vx-shell min-h-screen"><aside className="fixed inset-y-0 left-0 hidden w-64 border-r border-white/6 bg-[#0a0c0f]/95 px-4 py-5 lg:block"><div className="mb-7 px-2"><div className="flex items-center gap-2"><div className="grid size-8 place-items-center rounded-lg bg-white text-xs font-black text-slate-950">V</div><span className="text-sm font-black tracking-[.18em]">VECTOROPS</span></div><div className="mt-2 pl-10 text-[10px] uppercase tracking-[.22em] text-slate-600">{identity}</div></div><AppNav items={nav} current={current}/><div className="mt-7 rounded-2xl border border-white/6 bg-black/15 p-3 text-xs text-slate-500"><div className="flex items-center gap-2 text-slate-300"><span className="size-1.5 rounded-full bg-emerald-400"/>Supabase connected</div><p className="mt-2 leading-5">The browser only sees RLS-scoped application data. Privileged control stays server-side.</p></div><div className="absolute bottom-4 left-4 right-4"><LogoutButton/></div></aside><div className="lg:pl-64"><header className="sticky top-0 z-20 border-b border-white/6 bg-[#0b0d10]/85 backdrop-blur-xl"><div className="flex h-16 items-center gap-4 px-4 md:px-6"><button className="grid size-9 place-items-center rounded-xl border border-white/8 bg-white/[.03] text-slate-300 lg:hidden" aria-label="Open menu"><Menu size={18}/></button><div className="min-w-0 flex-1"><div className="truncate text-sm font-semibold text-white">{title}</div>{subtitle && <div className="truncate text-xs text-slate-500">{subtitle}</div>}</div><div className="hidden w-64 items-center gap-2 rounded-xl border border-white/7 bg-white/[.025] px-3 py-2 md:flex"><Search size={15} className="text-slate-500"/><span className="text-xs text-slate-600">Search</span><kbd className="ml-auto text-[10px] text-slate-600">⌘K</kbd></div><button className="hidden size-9 place-items-center rounded-xl border border-white/8 bg-white/[.03] text-slate-400 sm:grid" aria-label="Notifications"><Bell size={17}/></button><div className="hidden sm:grid size-9 place-items-center rounded-full bg-white/[.06] text-xs font-bold text-slate-300">{identity[0]}</div></div></header><main className="mx-auto max-w-[1600px] px-4 py-5 md:px-6 md:py-7">{children}</main>{mobileNav && <div className="fixed inset-x-3 bottom-3 z-30 rounded-2xl border border-white/8 bg-[#111419]/95 p-1.5 shadow-2xl backdrop-blur-xl lg:hidden"><AppNav items={nav.slice(0,6)} current={current} mobile/></div>}</div></div>;
}
