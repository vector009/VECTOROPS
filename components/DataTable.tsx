export function DataTable({ headers, rows }: { headers:string[]; rows:React.ReactNode[][] }) { return <div className="overflow-hidden rounded-2xl border border-white/7 bg-white/[.015]"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-white/7 bg-black/15 text-[10px] uppercase tracking-[.14em] text-slate-500"><tr>{headers.map(h=><th key={h} className="px-4 py-3 font-semibold">{h}</th>)}</tr></thead><tbody className="divide-y divide-white/6">{rows.map((row,i)=><tr key={i} className="hover:bg-white/[.02]">{row.map((cell,j)=><td key={j} className="px-4 py-3 align-middle text-slate-300">{cell}</td>)}</tr>)}</tbody></table></div></div> }
