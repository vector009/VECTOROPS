import Link from "next/link";
import { requireAdmin } from "@/lib/auth";
import { createClientOnboarding } from "@/lib/actions";
import { createClient } from "@/lib/supabase/server";
import { PageHeader } from "@/components/PageHeader";

export default async function NewClient() { await requireAdmin(); const supabase=await createClient(); const {data:instances}=await supabase.from("n8n_instances").select("id,instance_name,hosting_type,status").order("instance_name"); return <><PageHeader eyebrow="Clients" title="Add client" description="Create the tenant, portal, commercial record and optional infrastructure/account setup without exposing privileged secrets."/><form action={createClientOnboarding} className="grid gap-5 lg:grid-cols-2"><section className="vx-surface rounded-2xl p-5"><div className="vx-kicker">01 · Identity</div><div className="mt-4 grid gap-4 sm:grid-cols-2"><Field name="company_name" label="Company" required/><Field name="contact_name" label="Contact"/><Field name="email" label="Account email" type="email"/><Field name="phone" label="Phone"/><Field name="slug" label="Portal slug" placeholder="acme-dental" required/><div className="sm:col-span-2"><Field name="notes" label="Notes" textarea/></div></div></section><section className="vx-surface rounded-2xl p-5"><div className="vx-kicker">02 · Portal</div><div className="mt-4 grid gap-4 sm:grid-cols-2"><Field name="portal_title" label="Portal title" defaultValue="Client Portal"/><Field name="primary_color" label="Primary color" defaultValue="#111827"/><Field name="accent_color" label="Accent color" defaultValue="#2563eb"/></div></section><section className="vx-surface rounded-2xl p-5"><div className="vx-kicker">03 · Commercial</div><div className="mt-4 grid gap-4 sm:grid-cols-2"><Field name="service_name" label="Service"/><Field name="monthly_amount" label="Monthly amount" type="number" min="0" step="0.01" defaultValue="0"/><Field name="currency" label="Currency" defaultValue="INR"/><Field name="billing_day" label="Billing day" type="number" min="1" max="28" defaultValue="1"/></div><p className="mt-3 text-xs leading-5 text-slate-500">Subscription starts as pending; renewal state is not invented by the UI.</p></section><section className="vx-surface rounded-2xl p-5"><div className="vx-kicker">04 · Infrastructure & access</div><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2"><span className="text-xs font-semibold text-slate-300">Assign n8n instance</span><select name="n8n_instance_id" className="mt-2 w-full rounded-xl border border-white/8 bg-black/20 px-3 py-3 text-sm text-slate-200"><option value="">Not connected yet</option>{(instances||[]).map(i=><option key={i.id} value={i.id}>{i.instance_name} · {i.hosting_type} · {i.status}</option>)}</select></label><Field name="client_password" label="Client password" type="password" autoComplete="new-password"/><div className="sm:col-span-2 rounded-xl border border-amber-400/10 bg-amber-400/[.03] p-3 text-xs leading-5 text-amber-100/75">The password is accepted only by this trusted server action, used to create the real Supabase Auth user, then discarded. It is never stored in VectorOps tables, URLs or logs.</div></div></section><div className="lg:col-span-2 flex flex-wrap justify-end gap-2"><Link href="/admin/clients" className="rounded-xl border border-white/8 px-4 py-3 text-sm text-slate-300">Cancel</Link><button className="rounded-xl bg-white px-5 py-3 text-sm font-bold text-slate-950">Create client</button></div></form></> }
function Field(p:{name:string;label:string;type?:string;defaultValue?:string;placeholder?:string;required?:boolean;textarea?:boolean;min?:string;max?:string;step?:string;autoComplete?:string}){return <label><span className="text-xs font-semibold text-slate-300">{p.label}</span>{p.textarea?<textarea name={p.name} rows={4} defaultValue={p.defaultValue} placeholder={p.placeholder} required={p.required} className="mt-2 w-full rounded-xl border border-white/8 bg-black/20 px-3 py-3 text-sm text-slate-200 outline-none focus:border-white/20"/>:<input name={p.name} type={p.type||"text"} defaultValue={p.defaultValue} placeholder={p.placeholder} required={p.required} min={p.min} max={p.max} step={p.step} autoComplete={p.autoComplete} className="mt-2 w-full rounded-xl border border-white/8 bg-black/20 px-3 py-3 text-sm text-slate-200 outline-none focus:border-white/20"/>}</label>}
